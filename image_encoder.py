
import  torch
import time
def image_encoder(image):

    token_embeddings = torch.randn(1, 66, 1536)
    token_embeddings = token_embeddings.numpy()
    time.sleep(0.2)
    return token_embeddings




